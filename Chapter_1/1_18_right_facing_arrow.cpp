#include <iostream>
using namespace std;

int main() {
    int body;
    int head;

    cin >> body >> head;
    cout << "     " << head << endl;
    cout << "     " << head << head << endl;
    cout << body << body << body << body << body << head << head << head << endl;
    cout << body << body << body << body << body << head << head << head << head << endl;
    cout << body << body << body << body << body << head << head << head << endl;
    cout << "     " << head << head << endl;
    cout << "     " << head << endl;

    return 0;
}